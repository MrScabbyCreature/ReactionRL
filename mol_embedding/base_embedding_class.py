class BaseEmbeddingClass:
    def mol_to_embedding(self):
        # return the animal's age
        raise NotImplementedError()

    def atom_to_embedding(self):
        # check if animal is dead or alive
        raise NotImplementedError()